#
# .zstyles — zstyle settings for completion, history, plugins, antidote
#

# ── antidote ─────────────────────────────────────────────────────
zstyle ':antidote:home'  path "$ANTIDOTE_HOME"
zstyle ':antidote:repo'  path "$ZDOTDIR/.antidote"
zstyle ':antidote:*'     zcompile 'yes'

# ── completion ───────────────────────────────────────────────────
# Open the picker on the first Tab, including shared quoted prefixes.
zstyle ':completion:*' menu yes
zstyle ':completion:*' group-name ''
zstyle ':completion:*' verbose yes
zstyle ':completion:*:descriptions' format '[%d]'
zstyle ':completion:*' completer _complete _ignored
zstyle ':completion:*' matcher-list 'm:{a-zA-Z}={A-Za-z}'
zstyle ':completion:*' use-cache on
zstyle ':completion:*' cache-path "$XDG_CACHE_HOME/zsh/.zcompcache"

zstyle ':completion:*' squeeze-slashes true
zstyle ':completion:*' special-dirs ..
zstyle ':completion:*:git-checkout:*' sort false

# Right accepts a choice and continues completion (e.g. into a directory).
zstyle ':fzf-tab:*' continuous-trigger right
