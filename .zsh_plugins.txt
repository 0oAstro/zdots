#
# .zsh_plugins.txt - antidote plugin list
#

# Completion sources. .zshrc runs compinit once after this bundle adds fpath.
zsh-users/zsh-completions kind:fpath path:src
zetlen/zsh-completion-generators kind:fpath
Aloxaf/fzf-tab kind:fpath

# Prompt
romkatv/powerlevel10k path:powerlevel10k.zsh-theme

# Editor / typing
romkatv/zsh-no-ps2

# Extra git subcommands (sourced from mattmc3's repo)
mattmc3/git-cmds kind:path

# Autosuggestions (asynchronous by default on supported zsh versions)
zsh-users/zsh-autosuggestions

# History substring search (binds keys via post: hook)
zsh-users/zsh-history-substring-search post:bindkey-hss

# vi: ft=properties
